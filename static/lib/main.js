'use strict';

/* globals document, $ */

$(document).ready(function () {
	// console.log('nodebb-plugin-user-level: loaded')
	// Note how this is shown in the console on the first load of every page
});
